{
  "name": "alwatr-nanolib",
  "description": "Necessary library for all ECMAScript (JavaScript/TypeScript) projects.",
  "author": "S. Ali Mihandoost <ali.mihandoost@gmail.com> (https://ali.mihandoost.com)",
  "devDependencies": {
    "@lerna-lite/changed": "^4.11.5",
    "@lerna-lite/cli": "^4.11.5",
    "@lerna-lite/diff": "^4.11.5",
    "@lerna-lite/exec": "^4.11.5",
    "@lerna-lite/publish": "^4.11.5",
    "@lerna-lite/run": "^4.11.5",
    "@lerna-lite/version": "^4.11.5",
    "conventional-changelog-conventionalcommits": "^9.3.0",
    "conventional-commits-filter": "^5.0.0",
    "typescript": "^5.9.3"
  },
  "license": "MPL-2.0",
  "prettier": "@alwatr/prettier-config",
  "private": true,
  "repository": "https://github.com/Alwatr/nanolib",
  "scripts": {
    "b": "bun run build",
    "build": "lerna run build",
    "c": "bun run clean",
    "cb": "bun run clean && bun run build",
    "clean": "git add -v . && git clean -d -x -f --exclude=node_modules --exclude='*.env' --exclude=_data",
    "f": "bun run format",
    "format": "bun run format:prettier && bun run format:eslint",
    "format:eslint": "bun run lint --fix",
    "format:prettier": "prettier . --ignore-path .gitignore --write",
    "l": "bun run lint",
    "lint": "bun run lint:ts && bun run lint:es",
    "lint:es": "echo eslint-skipped",
    "lint:es2": "eslint . --config .eslintrc.json --ext .ts",
    "lint:ts": "lerna run build:ts",
    "publish": "lerna publish from-package",
    "pull": "git pull",
    "release": "lerna version",
    "rl": "bun run pull && bun run clean && bun run build && bun run lint && bun run test && bun run release",
    "rlt": "bun run release --dry-run",
    "t": "bun run test",
    "test": "echo test-skipped",
    "w": "bun run watch",
    "watch": "lerna run --parallel watch"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
